# DataBye landing

Sitio estático de producto para la rama `landing` (Coolify app `databy`).

Sirve `index.html` + `wizard.html` con nginx alpine. El código auditable (skill, roster, playbooks) vive en `main`: https://github.com/686f6c61/DataBye

Versión de producto: **2.0.0**. MIT.
